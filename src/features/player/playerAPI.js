import { instance } from "../../utils/axios";

export function handlePIN(pin) {
  // call api || websocket
  return true;
}

export function handleNAME(name) {
  // call api || websocket
  return true;
}

